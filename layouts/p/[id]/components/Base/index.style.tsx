import styled from 'styled-components';

export const PostPageBaseWrapper = styled.section`
  border-radius: var(--border-radius);
  padding: 18px;

  @media (max-width: 574px) {
    margin: 0 -15px;
  }
`;
